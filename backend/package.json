{
  "name": "@stage/backend",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "esbuild src/index.ts --bundle --outdir=dist --format=esm --external:cloudflare:*",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "hono": "^4.7.0",
    "@stage/shared": "*"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250214.0",
    "esbuild": "^0.25.0",
    "typescript": "^5.7.3"
  }
}
